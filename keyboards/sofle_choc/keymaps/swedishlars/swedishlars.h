// TODO rename file, mv to lib?

#pragma once

enum custom_keycodes {
    KC_QWERTY = SAFE_RANGE,
    KC_LOWER,
    KC_RAISE,
    KC_ADJUST,
    KC_KEYLOG
};

enum sofle_layers {
    _DEFAULT = 0,
    _QWERTY = 0,
    _LOWER,
    _RAISE,
    _NUMPAD,
    _MOUSE,
    _ADJUST
};
